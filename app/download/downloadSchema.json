{
  "title": "Links to be displayed on the downloads page",
  "type": "array",
  "items": {
    "title": "A group of buttons to be generated. Keys are titles, values are objects with url and optionally icon",
    "type": "object",
    "additionalProperties": {
      "title": "Key is title, value is object with url and optionally with icon",
      "type": "object",
      "required": [ "url" ],
      "properties": {
        "icon": {
          "type": "string"
        },
        "url": {
          "type": "string",
          "format": "uri",
          "pattern": "^https?://"
        }
      }
    }
  }
}